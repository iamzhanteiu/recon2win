# targets/

Per-target working area. Each `run` creates `targets/<target>/raw/` holding
the acquired JS bodies. Copy `_template/{scope.md,config.json}` to
`targets/<target>/` to record program scope and per-target overrides.
